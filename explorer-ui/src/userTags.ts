// Stub for the viewer's tag-resolution store. Task 13 replaces this with the
// real module-store (mirroring live.ts/session.ts) that indexes the tag map's
// addresses so pills can resolve a member tag without a lookup per address.
// eslint-disable-next-line @typescript-eslint/no-unused-vars -- signature fixed now, body arrives in Task 13
export function setTagMap(_map: unknown): void {}
